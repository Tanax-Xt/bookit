import Root, {
  TooltipRootBaseProps as RootBaseProps,
  TooltipRootProps as RootProps,
  TooltipRootVariantProps as RootVariantProps,
} from "./Root";
import styles from "./styles";

export type { RootBaseProps, RootProps, RootVariantProps };

export { Root, styles };

const Tooltip = Root;

export default Tooltip;
